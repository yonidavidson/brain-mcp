# Storage URL Configuration
# Supported formats:
# - File-based (default): file://path/to/database.duckdb
# - S3: s3://bucket-name/path/to/data
# - GCS: gcs://bucket-name/path/to/data

STORAGE_URL=file://brain-memory.duckdb

# AWS credentials (required for S3 storage)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_REGION=us-east-1

# GCS credentials (required for GCS storage)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Memory Consolidation Configuration
# OpenAI API configuration (required for memory consolidation)
OPENAI_API_KEY=your-openai-api-key

# OpenAI API base URL (optional, for OpenAI-compatible APIs)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Model to use for consolidation (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Cron schedule for nightly consolidation (default: 0 0 * * * = midnight daily)
# Format: minute hour day month weekday
# CONSOLIDATION_SCHEDULE=0 0 * * *

# Enable/disable automatic consolidation (default: true)
# ENABLE_CONSOLIDATION=true
